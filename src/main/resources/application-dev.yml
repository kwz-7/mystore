server:
  port: 8005
  servlet:
    context-path: /test
spring:
  devtools:
    remote:
      restart:
        enabled: false
  datasource:

    url: jdbc:mysql://127.0.0.1:3306/db_1
    data-username: root
    data-password: root123
    tomcat:
           max-idle : 10
           max-active: 50
           max-wait : 10000
           initial-size : 5
           default-transaction-isolation : 2
mybatis :
    mapper-locations : classpath:mapper/*.xml
    type-aliases-package : com.mystore.entity


 
